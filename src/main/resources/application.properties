# --- CONEXIÓN A BASE DE DATOS ---
spring.datasource.url=jdbc:mysql://localhost:3306/techflow_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
# ? PON TU CONTRASEÑA REAL AQUÍ ABAJO
spring.datasource.password= Rfbp31102006@@

# --- CONFIGURACIÓN DE JPA (Hibernate) ---
# Esto creará las tablas automáticamente cuando definamos las Entidades
spring.jpa.hibernate.ddl-auto=update
# Muestra el SQL en la consola (útil para aprender)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- PUERTO DEL SERVIDOR ---
server.port=8080
# --- SEGURIDAD JWT ---
# Esta clave debe ser MUY larga y secreta (mínimo 256 bits para HS256)
jwt.secret=TechFlowSecretKeyParaFirmarTokensMuySegura123456789
# Tiempo de vida del token (86400000 ms = 1 día)
jwt.expiration=86400000