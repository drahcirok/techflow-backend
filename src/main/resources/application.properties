# --- CONEXI�N A BASE DE DATOS ---
spring.datasource.url=jdbc:mysql://localhost:3306/techflow_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
# ? PON TU CONTRASE�A REAL AQU� ABAJO
spring.datasource.password= 12345

# --- CONFIGURACI�N DE JPA (Hibernate) ---
# Esto crear� las tablas autom�ticamente cuando definamos las Entidades
spring.jpa.hibernate.ddl-auto=update
# Muestra el SQL en la consola (�til para aprender)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# --- PUERTO DEL SERVIDOR ---
server.port=8080

# --- CONFIGURACIÓN DE UPLOAD DE ARCHIVOS ---
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
# --- SEGURIDAD JWT ---
# Esta clave debe ser MUY larga y secreta (m�nimo 256 bits para HS256)
jwt.secret=TechFlowSecretKeyParaFirmarTokensMuySegura123456789
# Tiempo de vida del token (86400000 ms = 1 d�a)
jwt.expiration=86400000